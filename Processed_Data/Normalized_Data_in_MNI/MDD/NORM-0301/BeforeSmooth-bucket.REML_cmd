# 3dDeconvolve -float -polort A -errts 0301-BeforeSmooth-ld -bucket BeforeSmooth-bucket -input 0301-BOLD-EC-volreg-Nwarp+tlrc.

3dREMLfit -matrix BeforeSmooth-bucket.xmat.1D -input 0301-BOLD-EC-volreg-Nwarp+tlrc. \
 -Rbuck BeforeSmooth-bucket_REML -Rvar BeforeSmooth-bucket_REMLvar \
 -Rerrts 0301-BeforeSmooth-ld_REML -verb $*
