# 3dDeconvolve -float -polort A -errts 0323-BeforeSmooth-ld -bucket BeforeSmooth-bucket -input 0323-BOLD-EC-volreg-Nwarp+tlrc.

3dREMLfit -matrix BeforeSmooth-bucket.xmat.1D -input 0323-BOLD-EC-volreg-Nwarp+tlrc. \
 -Rbuck BeforeSmooth-bucket_REML -Rvar BeforeSmooth-bucket_REMLvar \
 -Rerrts 0323-BeforeSmooth-ld_REML -verb $*
