# 3dDeconvolve -float -polort A -errts 0285-BeforeSmooth-ld -bucket BeforeSmooth-bucket -input 0285-BOLD-EC-volreg-Nwarp+tlrc.

3dREMLfit -matrix BeforeSmooth-bucket.xmat.1D -input 0285-BOLD-EC-volreg-Nwarp+tlrc. \
 -Rbuck BeforeSmooth-bucket_REML -Rvar BeforeSmooth-bucket_REMLvar \
 -Rerrts 0285-BeforeSmooth-ld_REML -verb $*
