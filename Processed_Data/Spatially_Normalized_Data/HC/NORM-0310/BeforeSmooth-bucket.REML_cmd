# 3dDeconvolve -float -polort A -errts 0310-BeforeSmooth-ld -bucket BeforeSmooth-bucket -input 0310-BOLD-EC-volreg-Nwarp+tlrc.

3dREMLfit -matrix BeforeSmooth-bucket.xmat.1D -input 0310-BOLD-EC-volreg-Nwarp+tlrc. \
 -Rbuck BeforeSmooth-bucket_REML -Rvar BeforeSmooth-bucket_REMLvar \
 -Rerrts 0310-BeforeSmooth-ld_REML -verb $*
