# 3dDeconvolve -float -polort A -errts 0331-BeforeSmooth-ld -bucket BeforeSmooth-bucket -input 0331-BOLD-EC-volreg-Nwarp+tlrc.

3dREMLfit -matrix BeforeSmooth-bucket.xmat.1D -input 0331-BOLD-EC-volreg-Nwarp+tlrc. \
 -Rbuck BeforeSmooth-bucket_REML -Rvar BeforeSmooth-bucket_REMLvar \
 -Rerrts 0331-BeforeSmooth-ld_REML -verb $*
